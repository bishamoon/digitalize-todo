import 'package:flutter/material.dart';

class NewTodo extends StatelessWidget {
  const NewTodo({Key? key}) : super(key: key);
  static const screenRoute = '/new_todo';
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
